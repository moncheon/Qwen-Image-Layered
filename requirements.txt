# Qwen-Image-Layered Requirements
# Python 3.11 recommended

# PyTorch (CUDA 12.4) - install first
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Diffusers (git version required for QwenImageLayeredPipeline)
diffusers @ git+https://github.com/huggingface/diffusers.git

# Core dependencies
transformers>=4.51.3
accelerate>=1.12.0
safetensors>=0.7.0
huggingface-hub[hf_xet]>=0.36.0

# Quantization
bitsandbytes>=0.49.0

# Attention optimization (optional)
xformers>=0.0.29
triton-windows>=3.2.0  # Windows only

# UI
gradio>=6.0.0

# Export formats
python-pptx>=1.0.0
psd-tools>=1.12.0
Pillow>=10.0.0

# Utils
numpy>=1.26.0,<2.0.0  # numpy 2.x incompatible with torch 2.6
